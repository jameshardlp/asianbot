[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "asian-bot"
version = "1.0.0"
dependencies = [
    "aiogram>=3.0.0",
    "requests>=2.31.0",
    "aiohttp>=3.9.0",
]
requires-python = ">=3.8"
